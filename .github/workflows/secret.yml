name: Secret test

on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo $SECRET_KEY
        env:
          DEFAULT_BRANCH: main
          SECRET_KEY: ${{secrets.MY_KEY}}
